name: Swift

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
      - name: Test
        run: swift test
      - name: Release build
        run: swift build -c release
